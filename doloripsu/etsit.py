# Example dictionary
my_dict = {'a': 1, 'b': 2, 'c': 3, 'd': 2}

# Find duplicate in dictionary
duplicate = find_duplicate_in_dict(my_dict)

if duplicate is not None:
    print(f"Duplicate item found: {duplicate}")
else:
    print("No duplicates found.")
